# PMID 39303722 Live Compatibility Report (`researcher-ai` v2.3.0)

## Scope
Validated the portal against `researcher-ai` installed from GitHub tag `v2.3.0`, ran the full automated test suite, and executed a live PMID parse for `39303722` using `LLM_API_KEY` from the setup env script.

## Environment commands used
1. `source ~/miniconda3/etc/profile.d/conda.sh`
2. `conda activate researcher-ai-portal`
3. `source ~/Documents/work/01_active/researcher-ai-portal_setup.sh`
4. `pip install --upgrade "git+https://github.com/byee4/researcher-ai.git@v2.3.0"`
5. `python -m pytest -q`
6. Live run command executed via Django context:
   - `DJANGO_SETTINGS_MODULE=researcher_ai_portal.settings python - <<'PY' ... _dispatch_workflow_step(...) ... PY`

## Version verification
- Distribution metadata version: `2.3.0`
- `researcher_ai.__version__`: `2.2.3` (upstream version-string drift in the package)

## Full test-suite result
- Command: `python -m pytest -q`
- Result: `146 passed in 7.99s`
- Compatibility status: automated tests pass after upgrade.

## Live PMID run summary (`39303722`)
- Model: `gpt-5.4`
- API key source: `LLM_API_KEY` environment variable
- Job ID: `4a2b7cf57f7d4d7ba676cc32604baf2c`
- Duration: `12.649s`
- Final status: `failed`
- Failed step: `paper`
- Error: `attempt to write a readonly database`

## Failures observed
1. Network transport retries during PubMed/PMCID fetch:
   - `WARNING HTTP transport error on attempt 1/2, retrying…`
   - `WARNING PubMed fetch failed for 39303722: [Errno 8] nodename nor servname provided, or not known`
   - `WARNING PMID→PMCID re-resolution failed for 39303722: [Errno 8] nodename nor servname provided, or not known`
2. Terminal write failure after paper parse completed:
   - Parse logs include `Paper parsing complete`
   - Step then fails with `Step failed: Paper Parser: attempt to write a readonly database`

## Fallbacks observed
- Parser retry fallback was active for network transport errors (multiple retries logged before hard failure).
- No vision-model fallback metadata was recorded:
  - `vision_fallback_count`: `null`
  - `vision_fallback_latency_seconds`: `null`
- No `needs_human_review` fallback path was triggered for this run.

## Artifacts
- Structured live-run output: `/tmp/pmid_39303722_live_test_2026-04-10.json`

---

## Rerun after database-path fix

### Fix applied
- Corrected default SQLite path in Django settings to project-local `template.db`:
  - from: `/Users/brianyee/Documents/work/01_active/template.db`
  - to: `/Users/brianyee/Documents/work/01_active/researcher-ai-portal/template.db`

### Rerun result
- Command path: `_run_full_pipeline_sync(...)` (same pipeline path used by API submission background runner)
- Job ID: `c2264070-754c-4009-a8ea-ad319437d65a`
- Duration: `18.062s`
- Final status: `completed`
- Stage: `Workflow complete`
- Progress: `100`
- Error: empty

### Remaining runtime limitations (environmental)
- PubMed/PMCID and Hugging Face calls still logged DNS/network transport errors in this sandboxed run.
- Pipeline still completed, but produced sparse output:
  - `0` figures
  - `0` assays
  - `0` datasets
  - `0` software tools
  - `0` pipeline steps

### Rerun artifacts
- API-runner output (fixed): `/tmp/pmid_39303722_live_test_2026-04-10_rerun_api_fixed.json`
- Direct step-dispatch output: `/tmp/pmid_39303722_live_test_2026-04-10_rerun.json`
